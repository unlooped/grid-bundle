{# @var type \Unlooped\GridBundle\ColumnType\AbstractColumnType #}

{% extends '@UnloopedGrid/column_types/_column.html.twig' %}

{% block column_content %}
    <span class="badge badge-status-{% if type.options.classPrefix %}{{ type.options.classPrefix }}-{% endif %}{{ type.value(data) }}">
        {{ type.value(data) }}
    </span>
{% endblock %}
