{# @var grid \Unlooped\GridBundle\Model\Grid #}

{% block grid_list_row %}
    <tr>
        {% for column in grid.visibleColumns %}
            {% include column.template with {'column': column, 'data': data} only %}
        {% endfor %}
    </tr>
{% endblock %}
