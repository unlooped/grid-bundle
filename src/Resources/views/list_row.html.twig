{# @var grid \Unlooped\GridBundle\Model\Grid #}
{# @var type \Unlooped\GridBundle\ColumnType\ColumnTypeInterface #}

{% block grid_list_row %}
    <tr>
        {% for column in grid.visibleColumns %}
            {% include column.template with {'type': column, 'data': data} %}
        {% endfor %}
    </tr>
{% endblock %}
